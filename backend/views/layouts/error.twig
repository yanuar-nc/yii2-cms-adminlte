
{{ use('yii\helpers\Html') }}
{{ use('yii\widgets\Breadcrumbs') }}
{{ use('common\widgets\Alert') }}

{{ register_asset_bundle('backend/assets/AppAsset') }} 

{{ void(this.beginPage()) }}
<!DOCTYPE html>
<html lang="{{ app.language }}">
<head>
    <meta charset="{{ app.charset }}">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    {{ html.csrfMetaTags | raw }}
    <title>{{ Html.encode(this.title) }}</title>
    {{ void(this.head) }}
</head>
<body>

    <div class="error-page" style="margin: 7% auto;">
        <div class="lockscreen-logo">
                <a href="../../index2.html"><b>{{ this.params['project']['firstname'] }}</b>{{ this.params['project']['lastname'] }}</a>
        </div>
            <!-- User name -->
            {{ this.render('/partials/alert.twig') | raw }}

            {{ content | raw}}
          <div class="text-center">
                Copyright © 2014-2016 <b><a href="http://almsaeedstudio.com" class="text-black">Almsaeed Studio</a></b><br>
                All rights reserved
          </div>
    </div>


{{ void(this.endBody()) }}
</body>
</html>
{{ void(this.endPage()) }}
